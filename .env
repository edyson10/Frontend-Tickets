# true = usa datos mockeados en memoria (sin backend). false = consume la API real.
VITE_USE_MOCK=false

# URL base de la API REST (solo se usa cuando VITE_USE_MOCK=false)
VITE_API_URL=http://localhost:4000/api/v1
